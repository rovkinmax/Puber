# Plan Context Budget

Read `AGENTS.md`, `.kent/project-contract.md`,
`.kent/workflow-profile.toml`, the Puber workflow skill, and the task's
authoritative body/source/comments. Select one `work_kind`, then read only its
mapped Plan procedure.

Load feature-target, MCP, Serena, web, or design recipes only when the task
actually triggers them. Do not preload implementation recipes, Smoke, release,
or the full Android recipe tree.

Ledger entry: selected work kind, authoritative sources, plan/spec paths,
unresolved decisions, and exact instruction files read.
