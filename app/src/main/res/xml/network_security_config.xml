<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Основные домены KinoPub -->
        <domain includeSubdomains="true">api.service-kp.com</domain>
        <domain includeSubdomains="true">api.alador.space</domain>
        <domain includeSubdomains="true">kino.pub</domain>

        <!-- Локальные хосты -->
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>

        <!-- IP адрес вашего компьютера с Charles -->
        <domain includeSubdomains="true">192.168.1.163</domain> <!-- Замените на ваш IP -->

        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </domain-config>

    <!-- Debug конфигурация - доверяем всем сертификатам -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="@raw/charles" />
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <!-- Базовая конфигурация для release -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config> 